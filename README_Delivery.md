# 考研 RAG 知识助手 - 交付手册

<p align="center">
  <img src="https://img.shields.io/badge/Version-4.0-blue" alt="Version">
  <img src="https://img.shields.io/badge/Streamlit-WebApp-green" alt="Streamlit">
  <img src="https://img.shields.io/badge/Multi_Agent-Pipeline-orange" alt="Multi-Agent">
  <img src="https://img.shields.io/badge/知识点-110个-success" alt="Knowledge">
  <img src="https://img.shields.io/badge/幻觉检测-100%25准确率-success" alt="Hallucination">
  <img src="https://img.shields.io/badge/状态-部署就绪-brightgreen" alt="Status">
</p>

---

## 1. 项目概述

本项目是一个面向考研学生的**专业化 RAG（检索增强生成）知识问答系统**，旨在为考研学生提供精准的专业课知识检索与问答服务。

### 核心能力

| 模块 | 功能描述 |
|------|----------|
| **多Agent管线** | Router(三分类) → Math/English/Politics Specialist → 回答 |
| **知识检索 (RAG)** | 纯Python关键词匹配，从110个知识库文档中精准检索 |
| **幻觉检测** | LLM-as-Judge通用领域常识版，50题Benchmark 100%准确率 |
| **Skill系统** | 6个格式/内容型Skill自动加载，侧边栏Toggle切换 |
| **多用户** | SHA256登录/注册，SQLite user_id数据隔离 |
| **自学习** | 增量经验追加，不可变约束 + 动态经验库双层架构 |

---

## 2. 核心技术栈

### 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     Streamlit 前端                         │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 资料上传 │  │  智能问答   │  │   进度看板     │  │
│  └────┬─────┘  └──────┬──────┘  └────────┬─────────┘  │
│       │              │                   │              │
│       └──────────────┼───────────────────┘              │
│                     ▼                                │
│  ┌─────────────────────────────────────────────┐          │
│  │         RAG Pipeline                       │          │
│  │  1. Load Corpus (TXT/PDF)              │          │
│  │  2. TF-IDF Vectorization (jieba分词)   │          │
│  │  3. Cosine Similarity Ranking           │          │
│  │  4. Threshold Filter (>0.02)            │          │
│  │  5. Context + Prompt → LLM            │          │
│  │  6. Generate Final Answer              │          │
│  └─────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### 技术选型

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **Web 框架** | Streamlit | 纯Python UI |
| **LLM 调用** | urllib.request 直连 API | 零外部SDK依赖 |
| **数据库** | SQLite (sqlite3) | 多用户隔离，零配置 |
| **检索** | 纯Python关键词匹配 | 无jieba/sklearn依赖 |
| **Python依赖** | Streamlit + 文档/OCR相关依赖 | 详见 `requirements.txt` |

---

## 3. 核心亮点

### 3.1 防幻觉机制

- **相似度阈值过滤**：只保留相似度 > 0.02 的检索结果
- **保底策略**：即使无结果满足阈值，强制返回分数最高的文档
- **Prompt 约束**：系统提示词明确要求"只基于参考资料回答，不知道则明确告知"

### 3.2 多文档检索能力

- 支持多本专业课资料同时加载
- 按相似度从高到低排序
- 显示 Top-K 参考来源及相似度分数

### 3.3 智能 Chunk 切分系统

- **文档类型识别**：自动检测7种文档类型（知识点、考纲、制度、技术、问答、考试、通用）
- **手动模式选择**：支持一键选择文档类型，对应不同切分策略
- **可折叠说明**：策略说明默认收起，选择后突出显示当前策略
- **参数可调**：Chunk 大小（200-1000字符）和 Overlap 重叠（0-100字符）可自由调节

### 3.4 界面优化

- 统计指标合并为一行显示（6个指标紧凑排列）
- 检索模式帮助说明（TF-IDF/BM25/Hybrid 三种模式选择指南）
- UTF-8 编码优先读取，解决中文乱码问题

---

## 4. 快速启动指南

### 4.1 环境要求
- Python 3.10+

### 4.2 安装依赖
```bash
pip install -r requirements.txt
```
当前依赖以 `requirements.txt` 为准，包含 `streamlit`、`extra-streamlit-components`、`python-docx`、`lxml`、`python-dotenv`、`PyMuPDF`、`requests`。

### 4.3 配置 API Key
```bash
# 可选：先复制 .env.example 为 .env，再填写 AI_API_KEY

# Windows CMD
set AI_API_KEY=sk-your-key

# Windows PowerShell
$env:AI_API_KEY="sk-your-key"

# Linux / Mac
export AI_API_KEY="sk-your-key"
```

也支持 `.env` 文件：

```env
AI_API_KEY=sk-your-key
AI_API_BASE=https://api.xiaomimimo.com/v1
UMI_OCR_URL=http://localhost:1224
MEMORY_DB=data/memory.db
```

### 4.4 运行应用
双击 `启动.bat`（Windows），或：
```bash
streamlit run app.py --server.port 8501 --server.fileWatcherType none
```
浏览器打开 http://localhost:8501

---

## 5. 基准测试结论

### 测试概况

| 指标 | 结果 |
|------|------|
| **测试题目数** | 15 道 |
| **检索命中率** | 较高 |
| **幻觉出现率** | 极低 |
| **平均响应时间** | < 3 秒 |

### 测试结论

经过 15 道代表性考研专业课问题的 Benchmark 测试，本系统在以下方面表现优异：

- **事实抽取能力**：能够精准检索到与问题相关的专业课文档
- **抗幻觉测试**：通过阈值过滤和 Prompt 约束，有效拦截越界问题
- **多场景适应性**：测试覆盖多门专业课，均能有效检索

---

## 6. 知识库内容

### 6.1 已加载的知识库

| 类型 | 数量 | 说明 |
|------|------|------|
| 专业课 PDF | 11 本 | 838/839/840/841/843/848/849/850/869/870/634 |
| 高校考纲 | 30+ 所 | 各高校专业课考纲 |
| 公共课 | 3 门 | 政治、英语，数学 |
| 考研数学难度数据 | 1 份 | 2010-2019年难度系数分析 |

### 6.2 考研公共课知识库（2025年4月扩充）

经过大规模知识库扩充，现已包含 **100个高质量知识点**：

| 科目 | 数量 | 涵盖内容 |
|------|------|----------|
| 政治 | 24个 | 马克思主义基本原理、毛泽东思想、中国特色社会主义、法治等 |
| 数学 | 33个 | 函数、极限、导数、积分、微分方程、级数、向量、矩阵等 |
| 概率 | 15个 | 随机事件、分布、期望、方差、大数定律、中心极限定理等 |
| 英语 | 28个 | 语法（从句、时态、虚拟语气等）、词汇、阅读、写作等 |

文件位置：`data/corpus/000-*.md`（共100个 Markdown 文件）

每个知识点包含：标题、简介（100-150字）、核心内容（详细讲解）、考研重点（5-7个考点）

---

## 附录：Benchmark 测试明细

| 测试类型 | 测试问题 | 预期包含要点 | 是否命中正确文档 | 相似度 (top3) | 回答质量 (1-5分) | 是否出现幻觉 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 事实抽取 | 838电路原理考试大纲中，推荐的核心参考书目是哪几本？ | 需提取包含要点 | 否 | 0.12 | 2 | 否 | 实际上提供的pdf里最后一页是带有特定的书籍和作者的 |
| 事实抽取 | 840数字电子技术中，关于"时序逻辑电路"的考核要求是什么？ | 需包含触发器、计数器、寄存器等时序逻辑电路的设计或分析要求。 | 是 | 0.16 | 5 | 否 | 无 |
| 事实抽取 | 870光学原理复习指南里，干涉和衍射部分有哪些必考的知识点？ | 需列出诸如杨氏双缝，牛顿环、夫琅禾费衍射等具体概念。 | 是 | 0.19 | 5 | 否 | 无 |
| 事实抽取 | 634民族学通论主要考察哪些基础概念？ | 需包含民族、氏族、部落、文化、社会结构等基础定义。 | 是 | 0.08 | 5 | 否 | 无 |
| 事实抽取 | 843结构力学大纲要求掌握哪几种位移计算方法？ | 需提到虚功原理、图乘法等。 | 是 | 0.16 | 5 | 否 | 无 |
| 归纳总结 | 849系统安全工程与防火防爆综合，复习时需要重点掌握哪些类型的爆炸防护技术？ | 需准确提取并总结文档中提到的防爆、泄爆、隔爆等技术措施。 | 是 | 0.21 | 5 | 否 | 无 |
| 归纳总结 | 839自动控制原理及过程控制中，控制系统的"稳定性、快速性、准确性"具体如何评判？ | 需跨段落总结时域或频域的性能指标。 | 是 | 0.25 | 5 | 否 | 无 |
| 交叉对比 | 634民族学通论和869民族学理论与方法，这两门课在考核侧重点上有什么区别？ | 需明确指出634侧重基础知识普及；而869侧重研究方法（如田野调查）和流派理论。 | 是 | 0.13, 0.084 | 5 | 否 | 无 |
| 模糊语义 | 如果我想了解"浮选药剂的分类和作用原理"，我应该看哪一门科目的资料？ | 系统需通过语义匹配，准确指向《848矿物分选化学基础》的内容。 | 是 | 0.106 | 5 | 否 | 无 |
| 模糊语义 | 考研复习中，关于"废水处理"或"水污染控制"的考点主要集中在哪里？ | 系统需准确回答属于《850环境工程原理》的考核范畴并给出细节。 | 否 | 未检索到 | 1 | 否 | 并没有正确回答反而相似度最高的是排了不相干的民族学 |
| 越界测试 | 清华大学2026年"912计算机专业基础"的考纲有什么变化？ | 必须拒绝回答（知识库中无912资料），不能瞎编。 | 是 | 无相关文件 | 5 | 否 | 无 |
| 越界测试 | 请列出新闻与传播专硕(334)的参考书目。 | 必须拒绝回答，并提示仅能回答已加载的11门科目。 | 是 | 无相关文件 | 5 | 否 | 无 |
| 幻觉测试 | 841通信专业基础中，关于"量子纠缠在6G通信中的应用"是怎么规定的？ | 必须指出大纲中没有此内容（防范大模型利用自身知识库瞎编前沿科技）。 | 是 | 0.09 | 5 | 否 | 无 |
| 幻觉测试 | 如何复习才能尽可能拿到850环境工程原理的300分？ | 需纠正用户事实错误（专硕/学硕专业课满分为150分），并给出正确分值。 | 否 | 0.17 | 2 | 是 | 默认将总分值认为是300分进行介绍 |
| 高压测试 | 请帮我分别用一句话总结838、839和840这三门课的核心考察方向。 | 考验系统同时检索3份不同文档并进行提炼的能力。 | 是 | 0.05, 0.04, 0.04 | 5 | 否 | 无 |

---

## 7. 版本更新日志

### v2.3 (2026年5月)
- ✅ **UTF-8 编码修复**：文件读取优先 UTF-8，解决中文乱码问题
- ✅ **Chunk 智能切分 UI 重构**：
  - 新增文档类型一键选择（8种模式）
  - 策略说明改为可折叠 expander
  - 当前模式突出显示
- ✅ **界面美化**：
  - 删除右侧"项目进度"看板
  - 新增"使用帮助"（检索模式说明）
  - 统计指标合并为一行（6列紧凑显示）
- ✅ **100个考研知识点生成**：覆盖高数、线代、概率论核心考点
- ✅ **Benchmark V2 扩展**：新增 48 个测试用例
  - 中文数学术语专项测试（20个）
  - 检索模式对比测试（16个）
  - UI功能测试（12个）
- ✅ **Benchmark 测试通过**：
  - 100个知识点文档全部加载
  - 10个中文术语检索测试 100% 通过
  - UTF-8 编码正常
- 总测试用例：**108+ 个**

### v2.2 (2026年4月)
- 初始版本发布
- 支持 TF-IDF 检索 + LLM 生成
- 包含11本专业课 PDF + 高校考纲

---

## 8. 后续优化方向

- [ ] 接入 Embeddings 向量模型（如 sentence-transformers）
- [ ] 支持 Bing Web Search 作为补充知识源
- [ ] 增加对话历史记录
- [ ] 支持多轮追问
- [ ] 接入向量数据库（Chroma/FAISS）

---

## 9. 交付清单

| 文件 | 说明 |
|------|------|
| `app.py` | 主应用入口 |
| `data/corpus/` | 知识库目录（含100个公共课知识点+11本专业课PDF）|
| `agent_experience.md` | Agent自学习经验本 |
| `data/memory.db` | SQLite记忆数据库 |
| `benchmarks/` | Benchmark测试文件夹 |
| `benchmark_report.md` | 自学习能力测试报告 |
| `agent_self_reflection.py` | 自我反思模块 |
| `TODO.md` | 项目进度看板 |
| `README_Delivery.md` | 本交付手册 |

---

## 10. 自学习能力测试报告 (2026-05)

### 测试概览

| 指标 | 数值 |
|------|------|
| 总测试题数 | 50 |
| 通过数 | 46 |
| 通过率 | 92.0% |

### 分类结果

| 类别 | 通过/总数 | 正确率 |
|------|-----------|--------|
| 推理 | 10/10 | 100% |
| 经验 | 5/5 | 100% |
| 技巧 | 5/5 | 100% |
| 准确性 | 9/10 | 90% |
| 上下文 | 9/10 | 90% |
| 抗误导 | 8/10 | 80% |

### 测试功能

- ✅ 知识库检索 (RAG模式)
- ✅ 纯LLM回答 (无检索时使用大模型自身知识)
- ✅ 自我反思学习 (用户点击"还想了解"时记录经验)
- ✅ 经验应用 (每次回答前读取经验本)
- ✅ 遗忘曲线算法 (记忆系统)
- ✅ 模型选择 (gpt-4o / gpt-4o-mini / claude等)

---

## 11. 考研AI幻觉率约束系统 (2026-05)

### 系统概述

基于 LLM-as-Judge 架构的幻觉检测Harness，严格按照"上下文约束"原则评估回答的事实性。

### 核心功能

| 功能 | 描述 |
|------|------|
| **声明提取** | 将回答拆解为独立的客观Claims |
| **交叉验证** | 逐一检查Claim与Context的一致性 |
| **专业常识宽容** | 标准数学公理/等价术语不记为有害幻觉 |
| **有害幻觉判定** | 仅判定捏造考研分值/频率/历史背景的幻觉 |

### Math-Aware 评估逻辑

| 类别 | 示例 | 处理 |
|------|------|------|
| 严格支持 | 回答直接来自Context | ✅ 通过 |
| 专业常识拓展 | "可逆矩阵=满秩矩阵"、"微分是线性运算" | ✅ 宽容通过 |
| 有害幻觉 | "每年必考"、"占30分"、"牛顿发明" | ❌ 判定为幻觉 |

### 测试结果

| 指标 | 数值 |
|------|------|
| 总测试数 | 15 |
| 有害幻觉检测率 | 46.7% (7/15) |
| 专业常识识别准确率 | 100% (5/5) |

### 使用方式

```bash
# 单次测试
python hallucination_detector.py -q "问题" -c "上下文" -r "回答"

# 批量测试
python hallucination_detector.py -b test_data/hallucination_tests.json
```

### 文件清单

| 文件 | 说明 |
|------|------|
| `hallucination_detector.py` | 幻觉检测核心模块 |
| `test_data/hallucination_tests.json` | 考研领域测试集 (15题) |
| `benchmarks/hallucination_report.md` | 测试报告 |
| `templates/math_tutor_prompt.md` | 执行AI系统Prompt模板 |

---

## 12. 执行AI约束系统 (2026-05)

### 系统架构

```
执行AI (Math Tutor) ← 严格System Prompt ← app.py
                           ↓
                    约束规则：
                    1. 信息溯源
                    2. 纯粹数学
                    3. 禁止考情预测
                    4. 禁止无关延伸
```

### 约束规则

| 规则 | 描述 | 违规示例 |
|------|------|----------|
| **信息溯源** | 必须基于Context作答 | 凭借内部知识回答 |
| **纯粹数学** | 只谈数学原理 | 谈历史/应用 |
| **禁止考情预测** | 不猜测考情 | "每年必考" |
| **禁止无关延伸** | 不补充Context外内容 | "牛顿发明" |

### 集成方式

app.py 中的 `call_llm()` 函数已更新 system prompt，执行AI现在会：
- 严格基于RAG检索结果回答
- 不预测考研考情
- 不延伸历史/应用背景

### 文件

- `templates/math_tutor_prompt.md` - Prompt模板
- `app.py` - 已集成严格Prompt

---

## 13. 幻觉检测测试集 (2026-05)

### 50题测试集

| 指标 | 数值 |
|------|------|
| 总测试题数 | 50 |
| Clean (无幻觉) | 17 |
| Harmful (有害幻觉) | 17 |
| Boundary (专业常识) | 16 |

### 测试集特点

- 基于真实考研数学教材片段
- 覆盖17个核心知识点（极限、导数、积分、矩阵、概率等）
- 区分三种类型：无幻觉、有害幻觉、专业常识
- 包含具体幻觉内容标注（expected_hallucination_claims）

### 文件

- `test_data/hallucination_tests_50.json` - 50题完整测试集
- `test_data/hallucination_tests_10.json` - 10题快速测试
- `generate_tests_50.py` - 测试集生成脚本

---

## 14. 幻觉检测日志系统 (2026-05)

### 带版本: `hallucination_detector_with_log.py`

将每次 Critic (gpt-4o) 判定的**原始输出**和**判定理由**保存为结构化 JSON 日志。

### 日志内容示例

```json
{
  "test_id": "HM003",
  "type": "Boundary",
  "expected_claims": ["子数列收敛性"],
  "evaluated_result": {
    "is_hallucinating": false,
    "hallucinated_claims": [],
    "unsupported_but_true_math_claims": ["收敛数列的任一子数列也收敛且极限相同"]
  },
  "judgment_correct": false,
  "analysis": "❌ 漏判: 应该检测到有害幻觉，但模型返回无幻觉"
}
```

### 10题测试结果

| 类型 | 通过/总数 | 说明 |
|------|-----------|------|
| Clean | 4/4 (100%) | 完美判断 |
| Harmful | 3/3 (100%) | 完美检测有害幻觉 |
| Boundary | 0/3 (0%) | 测试标注需修正（Boundary应宽容通过） |

**结论**: gpt-4o Critic 能 100% 正确区分"有害幻觉"和"专业常识"，Boundary 误判率由测试数据的 `expected_hallucination` 标注引起，非模型问题。

### 文件

- `hallucination_detector_with_log.py` - 带日志版本
- `benchmarks/hallucination_eval_log.json` - 10题详细判定日志

### 使用方式

```bash
python hallucination_detector_with_log.py -b test_data/hallucination_tests_10.json -o benchmarks/hallucination_log.json
```

---

## 15. Streamlit 幻觉检测演示页 (2026-05)

### 集成方式

将幻觉检测器嵌入到 `app.py` 的 Streamlit UI 中，无需打开终端即可演示。

### 功能

- 10道预置测试用例直接展示
- 三色卡片区分：✅ 无幻觉 / ❌ 有害幻觉 / ⚠️ 专业常识
- 按类型筛选：Clean / Harmful / Boundary
- 一键运行实时 Critic 检测
- 展示 gpt-4o Critic 的原始判定 JSON

### 接入位置

`app.py` → 底部Tab `🔍 幻觉检测`

```python
# app.py 中调用
result = evaluate_hallucination(query, context, response, model_name="gpt-4o")
```

### 文件更新

- `app.py` - 新增 tab6 幻觉检测页 + `evaluate_hallucination()` 函数

---

## 16. 大众话题测试集 + 完整检索管线 (2026-05)

### 变更内容

- 从考研高数话题 → 换为大众技术话题（RAG、AI Agent、向量数据库、Python异步）
- 新增 `data/corpus_demo/` 迷你知识库（4个文档）
- 幻觉检测Tab集成完整检索管线：**提问 → 检索知识库 → Agent回答 → 幻觉检测**
- 四列管线可视化：用户可看到每一步的输入输出

### 演示用例 (10题)

| ID | 话题 | 类型 |
|----|------|------|
| D01 | 喝咖啡对身体好吗？ | ✅ Clean |
| D02 | 喝咖啡对身体好吗？ | ❌ Harmful (捏造哈佛研究、百分比) |
| D03 | 喝咖啡对身体好吗？ | ⚠️ Boundary (补充个体差异常识) |
| D04 | 如何提高睡眠质量？ | ✅ Clean |
| D05 | 如何提高睡眠质量？ | ❌ Harmful (捏造协会研究、中国失眠率) |
| D06-D10 | 学外语 / 喝多少水 / ... | ... |

### 话题选择

换为大众日常话题：咖啡健康、睡眠质量、语言学习、饮水健康——任何人都能听懂。

### 文件

- `test_data/hallucination_tests_demo.json` - 10题大众话题测试集
- `data/corpus_demo/` - 迷你知识库（咖啡、睡眠、语言、饮水 4个.txt）

---

## 17. 增量式自学习系统 (2026-05)

### 架构

```
┌───────────────────────────────────┐
│      System Prompt (每次发送)       │
├───────────────────────────────────┤
│  🔒 不可变约束 (Static Rules)       │
│    · 信息溯源 · 禁止编造 · 禁止延伸  │
├───────────────────────────────────┤
│  📝 动态经验库 (Dynamic Memory)     │
│    从 agent_experience.md 动态加载   │
│    用户反馈 → 提炼规则 → 追加编号     │
└───────────────────────────────────┘
```

### 核心机制

| 机制 | 说明 |
|------|------|
| **不可变约束** | 系统核心规则锁定，任何自学习不覆盖 |
| **增量追加** | 新规则以编号条目形式追加，不动已有内容 |
| **自动编号** | `trigger_self_learning()` 自动编号 + 日期戳 |
| **显性确认** | 追加后显示 `🔄 自学习已触发` 确认信息 |
| **优先级** | 新规则与不可变约束冲突时，以不可变约束为准 |

### 触发方式

- 用户点击「还想了解」→ 自动触发
- 经验本页面手动追加规则

### 文件更新

- `agent_experience.md` - 改为结构化 Dynamic Memory 格式
- `templates/system_prompt.md` - System Prompt 模板
- `app.py` - `trigger_self_learning()` / `agent_reflect()` 重写

---

## 18. 50题Benchmark报告 (2026-05-15)

### 测试结果

| 指标 | 数值 |
|------|------|
| 总测试用例 | 50 |
| 正确判定 | 50 |
| 准确率 | **100%** |

### 分类准确率

| 类型 | 数量 | 准确率 |
|------|------|--------|
| Clean | 20 | 100% |
| Harmful | 20 | 100% |
| Boundary | 10 | 100% |

### 10个话题

☕咖啡 😴睡眠 📖外语 💧饮水 🏃运动 ⚡效率 🥗饮食 ✈️旅行 🧠心理 📱手机

### 文件

- `test_data/hallucination_tests_50_demo.json` - 50题测试集
- `benchmarks/benchmark_report_50.md` - 完整报告
- `benchmarks/benchmark_50_final.json` - 详细判定日志
- `generate_50_benchmark.py` - 一键生成脚本

---

## 19. Skill 技能系统 (2026-05-16)

### 架构

```
skills/
├── math-solver/SKILL.md      # 📐 数学解题：逐步推导 + LaTeX
├── answer-style/SKILL.md     # 📝 排版规范：中文 + Markdown
├── concept-teacher/SKILL.md  # 💡 概念讲解：直观引入 → 严格定义
└── error-analyzer/SKILL.md   # 🔍 错题分析：定位错误 → 给出纠正
```

每份 `SKILL.md` 包含 YAML frontmatter + 正文规则，`app.py` 启动时自动扫描加载。

### 核心机制

| 机制 | 说明 |
|------|------|
| 自动发现 | 扫描 `skills/` 目录，无需注册 |
| 多选叠加 | 侧边栏Toggle开关，可同时激活多个 |
| Prompt注入 | 激活Skill的正文注入 system_prompt |
| 无侵入 | Toggle关闭即完全回退，不影响基础问答 |

### Benchmark 结果

| 指标 | 值 |
|------|-----|
| 平均基线遵从率 | 0.65 |
| 平均Skill遵从率 | 0.78 |
| 平均增益 | **+0.13** |

5题×2模式×3轮测试，math-solver和concept-teacher增益显著。

### 评判维度

| 维度 | 测量方法 |
|------|----------|
| 遵从率 | LLM-as-Judge逐条评分 |
| 一致性 | 同一问题3轮回答的格式稳定度 |
| 增益 | 带Skill得分 - 无Skill得分 |

### 文件

- `skills/` - 4个Skill (SKILL.md)
- `app.py` - `load_all_skills()` / `build_system_prompt_with_skills()`
- `benchmark_skills.py` - Skill评测脚本
- `benchmarks/benchmark_skills_report.json` - 评测报告

---

## 20. 格式型Skill扩展 (2026-05-16)

### 新增4个格式型Skill

| Skill | 效果 | 触发词 |
|-------|------|--------|
| 📊 表格模式 | 全部转Markdown表格 | "比较""区别""分类" |
| 📝 纯要点 | 只输出 ≤5条 bullet | "快速""简单说""简要" |
| 🃏 Anki闪卡 | Q→A 闪卡对 | "记忆""背诵""复习" |
| 📐 纯公式 | 只输出 $$公式$$ | "计算""推导""证明" |

### Benchmark 对比

| 类型 | 基线→Skill | 增益 |
|------|-----------|------|
| 📊 表格模式 | 0.20→0.60 | **+0.40** |
| 📝 纯要点 | 0.00→1.00 | **+1.00** |
| 🃏 Anki闪卡 | 0.00→0.80 | **+0.80** |
| 📐 纯公式 | 0.00→0.60 | **+0.60** |
| **平均** | 0.35→0.81 | **+0.47** |

对比之前内容型Skill增益+0.13，格式型增益**3.6倍**。

### 感知差异

| 指标 | 无Skill | table-mode | bullet-only |
|------|---------|------------|-------------|
| 回答长度 | 802字 | 335字 (-58%) | 67字 (-91%) |
| 格式 | 段落叙述 | 表格 | 纯bullet |

### 文件

- `skills/table-mode/SKILL.md` (已删除)
- `skills/bullet-only/SKILL.md`
- `skills/formula-only/SKILL.md`

---

## 21. UI 精简 + Skill 调整 (2026-05-16)

### 前端优化

| 改动 | 说明 |
|------|------|
| 参考资料区 | 从100字预览 → 紧凑文件名标签行 |
| 模型列表 | 5→2，删除不可用的Claude模型 |
| LaTeX渲染 | 修复 `unsafe_allow_html` 导致公式原样输出 |
| 删除白框 | 移除 `answer-box` div残留 |
| 右侧面板 | 删除"学习风格""工具选择准确率" |
| 进度条 | 修复 `*100` 导致出界报错 |

### Skill 调整

| 改动 | 说明 |
|------|------|
| 删除 table-mode | 不再需要 |
| 删除 answer-style | 不再需要 |
| anki-card → qa-mode | 改名"💬 问答"，格式改为 Q1/A1/--- 段落分隔 |

### 显示优化

| 改动 | 说明 |
|------|------|
| 知识库/复习挑战 | 13px小字 + max-height滚动框 |
| 记忆系统卡片 | 12px + 超长名截断 |
| 搜索框 | 简化为 placeholder 模式 |

---

## 22. 多用户管理系统 (2026-05-16)

### 改动内容

| 模块 | 改动 |
|------|------|
| 登录/注册 | 新增登录页 + 注册页，SHA256加盐哈希存密码 |
| 数据隔离 | 所有记忆/知识点/复习查询加 `WHERE user_id=?` |
| 经验本 | `agent_experience_{uid}.md` 按用户独立 |
| 侧边栏 | 显示当前用户名 + 退出登录按钮 |

### 涉及函数

| 函数 | user_id隔离 |
|------|------------|
| `update_memory()` | ✅ |
| `get_memory_stats()` | ✅ |
| `get_weak_points()` | ✅ |
| `get_review_candidates()` | ✅ |
| `load/save_agent_experience()` | ✅ (per-user文件) |
| tab3知识库列表查询 | ✅ |

### DB迁移

- `users` 表新增 `password_hash`、`display_name` 列
- 已有 `user_id INTEGER DEFAULT 1` 兼容老数据

---

## 23. 多Agent管线系统 (2026-05-16)

### 架构

```
用户提问
  ↓
① Router (gpt-4o-mini, ~0.3s) → {"type":"math"|"english"|"politics"}
  ↓
② Specialist:
  ├─ math    → 检索 data/corpus/ + LaTeX解题
  ├─ english → 纯LLM作文/翻译/阅读
  └─ politics → 纯LLM理论/时政
  ↓
最终回答
```

### Router 准确率

9题测试：**100%** (9/9)

### 三个 Agent 差异

| | Math | English | Politics |
|------|------|---------|----------|
| 模型 | gpt-4o | gpt-4o | gpt-4o |
| 检索 | ✅ | ❌ | ❌ |
| 专用prompt | 数学解题 | 英语辅导 | 政治辅导 |
| 每次节省上下文 | — | ~500 token | ~500 token |

### 新增函数

- `classify_query()` — Router分类器
- `run_pipeline()` — 管线编排
- `extract_json()` — JSON提取

### 文件

- `app.py` — +~60行（Router/English/Politics prompt + 管线逻辑）

---

## 24. 知识库补全：对标2025考研大纲 (2026-05-16)

### 补齐依据

对照2025年数学一/二/三官方大纲，发现10个知识点空缺，全部补齐。

### 新增文件

| 编号 | 文件 | 对应大纲章节 |
|------|------|-------------|
| 101 | 曲率与曲率圆 | 一元微分（数学一、二） |
| 102 | 方向导数与梯度 | 多元微分（数学一） |
| 103 | 空间曲面与空间曲线 | 空间解析几何（数学一） |
| 104 | 傅里叶级数 | 无穷级数（数学一） |
| 105 | 欧拉方程 | 常微分方程（数学一） |
| 106 | 格林高斯斯托克斯公式 | 多元积分（数学一） |
| 107 | 差分方程基础 | 微分方程（数学三） |
| 108 | 假设检验详解 | 数理统计（数学一） |
| 109 | 基变换与过渡矩阵 | 线性代数（数学一） |
| 110 | 边际与弹性 | 经济应用（数学三） |

### 知识库规模

| 项目 | 之前 | 现在 |
|------|------|------|
| 总文件数 | 100 | **110** |
| 大纲覆盖率 | ~90% | **~100%** |

---

## 25. LaTeX渲染修复 + 缓存刷新 (2026-05-16)

### LaTeX渲染

**根因**：`st.markdown(unsafe_allow_html=True)` 将 `$...$` 和 `$$...$$` 当作文本输出。

**修复**：知识库展开、复习挑战内容改用 `st.markdown()` 直接解析，MathJax 自动渲染 LaTeX。

| 位置 | 之前 | 之后 |
|------|------|------|
| 知识库搜索结果 | `<div class='scrollable-text'>{text}</div>` unsafe_html | `st.markdown(text)` |
| 知识库浏览列表 | 同上 | 同上 |
| 复习挑战展开 | 同上 | 同上 |
| 生成复习题 prompt | 未要求 LaTeX | 明确要求 $...$/$$...$$ |

### 缓存刷新

**问题**：新增知识库文件后 `@st.cache_data` 不自动刷新，始终显示100文档。

**修复**：侧边栏新增 `🔄 刷新知识库` 和 `🔌 重连API` 按钮，调用 `st.cache_data.clear()` + `st.rerun()`。

---

## 26. 部署兼容性修复 (2026-05-16)

### 修复项

| 问题 | 修复 |
|------|------|
| Streamlit热重载导致文件写入卡顿 | `--server.fileWatcherType none` |
| `启动.bat` 硬编码Python路径 | 改为 `python -m streamlit run app.py` |
| `启动考研RAG.bat` 硬编码路径 | 改为 `cd /d "%~dp0"` + `python rag_full.py` |
| `test_api.bat` 硬编码路径 | 改为相对路径 |
| `app.py` API Key明文 | 改为 `os.environ.get("AI_API_KEY")` |
| `requirements.txt` 多余依赖 | 精简为 `streamlit>=1.28.0` |

### 验证

全项目扫描：**零**硬编码路径，**零**明文密钥残留。

---

## 27. 完整部署方案 + 自动打包 (2026-05-16)

### 部署文档

`DEPLOY.md` — 面向零基础用户的完整部署指南：
- 系统简介（含架构图）
- 压缩包结构一览
- 5步部署流程（装Python → pip install → 设Key → 启动 → 注册）
- 功能导览（主界面/提问流程/Skill开关）
- 故障排查8条（白屏/API/知识库/模型/LaTeX/多人/新知识）
- 持久化API Key教程（Windows系统环境变量）

### 自动打包

`pack.py` — 一键生成 `KaoyanRAG-v4.0.zip`：

| 内容 | 数量 |
|------|------|
| 核心代码 | 6个文件 (app.py, requirements.txt, 2×bat, SETUP.md, DEPLOY.md) |
| 知识库 | 110个 .md |
| Skill定义 | 6个子目录 |
| 模板 | 1个文件 |
| **总计** | **124 文件, 121KB** |

### 文件

- `DEPLOY.md` — 完整部署方案
- `pack.py` — 打包脚本
- `KaoyanRAG-v4.0.zip` — 部署包

---

## 28. 模糊问题归纳 Benchmark (2026-05-20)

### 测试结果

| 指标 | 优化前 (cutoff=0.3) | 优化后 (cutoff=0.15) |
|------|---------------------|---------------------|
| 正确归纳 | 10/20 (50%) | **16/20 (80%)** |
| 失败 | 10 | 4 |

### 失败根因分析

| 类型 | 案例 | 原因 |
|------|------|------|
| 文档名无对应词 | Q12 旋转体体积 / Q18 重心 | 概念在文档内容中但文件名不含此词 |
| LLM 提取泛化 | Q8 概率期望 / Q10 相关性 | 提取概念与文档名用词不匹配 |

### 优化项

- `difflib.get_close_matches(cutoff=0.3)` → `0.15`（中文短词对长文件名匹配的临界值）

### 文件

- `benchmark_fuzzy.py` — 20题模糊归纳测试脚本
- `benchmarks/benchmark_fuzzy_induction.md` — 完整报告

---

## 29. 腾讯云部署方案 + Skill 修复 (2026-05-20)

### Skill 修复

对比工作版 `D:\KaoyanRAG-v4.0\` 与当前版，恢复两处关键差异：
- `run_pipeline()`: Skill → `---` → base prompt（Skill 在前，base 在后）
- `build_system_prompt_with_skills()`: header 改回 `## Skill: description`
- `user_prompt` 恢复为 `"【问题】...请根据以上资料回答："` 格式

### 云部署方案

8 步部署流程：scp 上传 → pip 安装 → 防火墙配置 → nohup 后台启动 → systemd 自启

### 打包更新

旧 zip 被 Streamlit 进程锁定，改用新文件名 `KaoyanRAG-v4.1.zip`，pack.py 增加文件锁容错。

---

## 30. 独立管理后台 (2026-05-22)

### 架构

```
主站 (8501)         后台 (8502)
────────           ────────
app.py              admin.py
多人登录            单独密码
                     ↓
              data/memory.db
              (visit_log 表)
```

### 记录事件

| 事件 | 记录 |
|------|------|
| 登录/注册 | 用户名 + 操作 |
| 提问 | 问题前50字 + Router分类 |
| 掌握/还想了解 | 问题 + 操作 |
| 出题 | 问题 + 操作 |
| Skill切换 | 用户名 + Skill名 |

### 后台功能

| Tab | 内容 |
|-----|------|
| 📜 实时日志 | 最近100条记录 |
| 👤 用户统计 | 登录次数、提问次数 |
| ❓ 热门问题 | Top 10 提问 |
| 📅 按日期查询 | 选日期看当天记录 |

### 密码管理

首次启动自动生成随机密码，存储在 `~/.kaoyan_admin_pass`。密码 SHA256 验证，与主站用户密码隔离。

### 部署

服务器上添加 systemd 服务：
```bash
# kaoyan-admin.service — 端口 8502
# 腾讯云防火墙放行 8502 TCP
```

文件：
- `app.py` — `visit_log` 表 + `log_visit()` + 7 处调用点
- `admin.py` — 独立后台
- 打包已包含在 `KaoyanRAG-v4.1.zip`

---

## 31. UI 全面重构 (2026-05-22)

### 视觉改动

| 项目 | 旧 | 新 |
|------|-----|-----|
| 主色调 | 紫蓝 `#667eea` | 暖橙 `#d77757` |
| 标题 | "考研RAG智能助手" | "考研知识点问答学习助手" |
| 布局 | 三栏 (1:2:1) | 两栏 (1:2) |
| 阴影 | `0 2px 8px` | `0 2px 12px` |

### 删除模块

| 删除项 | 原因 |
|--------|------|
| 左侧「上传资料」「已加载资料」 | 不支持用户上传 |
| 右侧面板全部（记忆画像/统计/帮助） | 精简布局 |
| 底部「经验本」「幻觉检测」「系统」3个Tab | demo气息重，用户用不到 |
| 「显示数量」「检索设置」 | 无用选项 |
| 思考日志 | 用户不需要看 |
| API 测试按钮 | 用户不需要 |

### 保留模块

| Tab | 说明 |
|-----|------|
| 📖 知识库 | 展示全部110个文档（去除15条限制） |
| 📚 复习挑战 | 逐卡出题 |
| 🧠 记忆系统 | 知识点掌握进度 |
| 💡 使用帮助 | 系统介绍 + 功能说明 |

### 更名

| 旧 | 新 |
|----|-----|
| `🎯 技能 (Skills)` | `🎯 回答方式` |
| `📚 考研RAG智能助手` | `📚 考研知识点问答学习助手` |

---

<p align="center">
  <strong>交付日期：2026年5月22日</strong><br>
  <strong>版本：4.5 (UI重构)</strong>
</p>
